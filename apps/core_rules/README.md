# core_rules

这是 `刺杀苏丹王 KillSultan` 的核心机制目录。

## 定位

这里负责：

- 规则真值
- 判定
- 回合推进
- 胜负结算
- 核心机制接口

## 维护边界

- 本目录默认由另一位人类开发者维护。
- 对开发者 `lxh` 来说，除非明确说明，否则后续开发不应主动改动这里。
- 对 AI 来说，除非 `lxh` 在当前任务中明确授权，否则不应主动修改本目录。

## 协作原则

- 外围适配、通信、动画、数据库、前端表现等，应尽量通过接口与本目录交互，而不是直接侵入规则实现。
- 任何需要修改本目录接口的工作，都应先与人类维护者对齐。
